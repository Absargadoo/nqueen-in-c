//alogorithm
Nqueens(k,n)
{
    for(i=1;i<=n;i+=)
    {
        if(place(k,n));
        x[k]=i;
        (k==n);
        print x([1:n])
    }
    else
    nqueen(k+1,n)
    {
        place(k,n);
        if(x[k]=i) || abs(x[j]-i=abs(j-k))
        return false;
    }
return true;
}
